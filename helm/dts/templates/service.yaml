apiVersion: v1
kind: Service
metadata:
  name: {{ include "dts.fullname" . }}
  labels:
    {{- include "dts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dts.selectorLabels" . | nindent 4 }}
