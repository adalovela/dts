apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dts.fullname" . }}-test-connection"
  labels:
    {{- include "dts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
